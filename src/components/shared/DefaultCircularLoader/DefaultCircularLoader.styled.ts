import { Box, styled } from '@mui/material';

export const StyledDefaultCircularLoaderRoot = styled(Box)(() => ({
  position: 'absolute',
  top: '50%',
  left: '50%',
  transform: 'translate(-50%, -50%)',
}));
