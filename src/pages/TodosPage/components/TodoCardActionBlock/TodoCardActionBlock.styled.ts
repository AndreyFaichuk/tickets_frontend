import { IconButton, styled } from '@mui/material';

export const StyledTodoCardActionBlockIcon = styled(IconButton)(() => ({
  padding: 0,
  backgroundColor: 'transparent',
  '&:hover': {
    backgroundColor: 'transparent',
  },
}));
