import { Stack, styled } from '@mui/material';

export const StyledEditTodoPageRoot = styled(Stack)(({ theme }) => ({
  backgroundColor: theme.palette.secondary.light,
  padding: theme.spacing(2),
}));
